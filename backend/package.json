{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "terminal": "node src/server.js --terminal",
    "dev:terminal": "node --watch src/server.js --terminal",
    "db:migrate": "node scripts/migrate.js",
    "db:check": "node -e \"require('./src/config/db').checkConnection().then(() => { console.log('PostgreSQL conectado'); return require('./src/config/db').close(); }).catch(error => { console.error(error.message); process.exit(1); })\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Seidel",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/websocket": "^11.2.0",
    "@whiskeysockets/baileys": "^7.0.0-rc.9",
    "dotenv": "^17.4.2",
    "fastify": "^5.8.4",
    "pg": "^8.22.0",
    "pino": "^9.9.0",
    "qrcode-terminal": "^0.12.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
